// export function insufficientStockMessage() {
  // const storedBricks = JSON.parse(window.localStorage.getItem('basket'));
  // console.log(e.target.nextSibling.value);
// 
  // for (const storedBrick of storedBricks) {
    // if (storedBrick.stock < 0) {
      // console.log('problem area');
      // window.confirm(`There is insufficient stock of the ${storedBrick.name} to process your order. Requested Quantity:${storedBrick.count}, Available Quantity: ${storedBrick.stock}. Please press OK if you would like to add all the available bricks to your shopping cart. Or, press Cancel.`);
// 
      // if (window.confirm == true) {
        // storedBrick.count = storedBrick.stock;
        // window.localStorage.setItem('basket', JSON.stringify(storedBricks));
      // }
    // }
  // }
// }
// 